// 타입 추론없이 배열선언
const strArr: string[] = ["1", "2", "3", "4", "5", "6"];
const numArr: number[] = [1, 2, 3, 4, 5, 6];

// 튜플
// 각 배열의 자리에 타입을 지정

const tuple: [string, number, object] = ["hello", 2, {}];
